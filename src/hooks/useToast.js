/**
 * useToast.js
 *
 * Re-exports the useToast hook from the Toast component for a cleaner import path.
 *
 * Usage:
 *   import useToast from '@/hooks/useToast';
 *   const { showToast } = useToast();
 */

export { useToast as default } from '@/components/layout/Toast';
